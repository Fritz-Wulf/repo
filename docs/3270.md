# FRITZ!Box WLAN 3270

The machine-readable profile covers the 3270 v2/v3 family. Exact variant detection is mandatory before firmware, recovery, or package operations.

## Historical Fritz.Wulf build evidence

A surviving extracted 3270 v3 build payload identifies FRITZ!OS 05.54 and Freetz-NG `freetz-ng-766MF-acc1f895ff`, built on 2026-08-27. Its userspace is ELF 32-bit LSB MIPS32 with uClibc 0.9.32.1. Hashes of the surviving metadata and BusyBox payload are recorded in `devices/3270/device.json`.

The matching custom image file and a flash/boot log were not found in the current inventory. Therefore the historical build evidence is marked `HISTORICAL VERIFIED`, while the overall family profile remains `INFERRED` and `flash_support` remains profile-only.

The surviving stock 3270 v3 05.54 image is recorded by size and SHA-256 as a reference artifact only and is not published by this repository.
