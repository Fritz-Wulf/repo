# Building

Repository metadata generation is intentionally independent from firmware builds. Run `python3 scripts/build_repository.py`, then `python3 scripts/validate_repository.py`.

Historical FRITZ!Box 7490 firmware reproduction belongs on BMAX-B6 using the preserved Freetz-NG tree and recorded config/patchset. Do not silently substitute another host or newer Freetz commit.
