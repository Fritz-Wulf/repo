# Development

Use a dedicated branch for each work package. Before edits inspect status, branch, recent log, and remotes. Preserve foreign changes and do not use destructive reset/clean operations.

Validation labels are `VERIFIED`, `HISTORICAL VERIFIED`, `INFERRED`, and `UNKNOWN`. Only promote a claim when evidence supports the stronger label.
