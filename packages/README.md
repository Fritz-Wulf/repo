# Packages

Published package artifacts live here together with machine-readable metadata. Unknown-provenance binaries are not accepted.

Historical upstream snapshots may be catalogued before packaging. They become installable only after a package definition, dependency/ABI metadata, compatibility declaration, reproducible build output and integrity metadata are present.
